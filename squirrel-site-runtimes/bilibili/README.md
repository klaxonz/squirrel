Bilibili Plugin
===============

Implements subscription crawling for bilibili using the monorepo runtime helpers.

Install in your backend environment:

```
pip install -e ./squirrel-site-runtimes/bilibili
```


